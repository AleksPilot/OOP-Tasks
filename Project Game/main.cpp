#include <iostream>
#include <string>
#include <algorithm>
#include <random>
#include <map>
#include "controller.h"

using namespace std;





int main() {
    /*number a, b;
    a.reset();
    b.reset();
    a.generate();
    a.print();
    numValue temp;
    b = b.convert(1570);
    //b.print();
    temp.init();
    number c;
    cout << c.easy_search(a, 1, 1) << '\n';
    temp = temp.checkout(a, b);
    temp.print();*/
    start();

    return 0;
}
